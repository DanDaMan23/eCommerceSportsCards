<div class="card col-6" style="width: 18rem;">
  <%# <img src="..." class="card-img-top" alt="No Image"> %>
  <%# Ask Kyle how to check if an image is stored %>

  <% if (card.image.present?) %>
    <%= image_tag(card.image) %>
  <% else %>
    <div>
    </div>
  <% end %>

  <div class="card-body">
    <h5 class="card-title"><%= card.player.name %></h5>
    <p><strong>Team</strong>: <%= card.team.city %> <%= card.team.name %></p>
    <p><strong>Price</strong>: $<%= card.price %></p>
    <%= link_to "Details", card, class: "btn btn-primary" %>

    <% if @cart.include?(card) %>
      <%# <button type="button" class="btn btn-secondary" disabled>Add to cart</button> %>
      <%= link_to 'Remove Item', remove_from_cart_path(card), method: :delete, class: "btn btn-secondary" %>
    <% else %>
      <%= link_to "Add Item", add_to_cart_path(card.id), method: :post, class: "btn btn-primary" %>
    <% end %>


  </div>
</div>